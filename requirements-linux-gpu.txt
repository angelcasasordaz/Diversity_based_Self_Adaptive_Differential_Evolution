# Optional Linux + NVIDIA extension. The cross-platform CPU environment stays
# in requirements.txt and does not import CuPy unless gpu/hybrid is selected.
-r requirements.txt

# CUDA runtime/toolkit dependencies are resolved by the ctk extra.
cupy-cuda13x[ctk]==14.2.0
